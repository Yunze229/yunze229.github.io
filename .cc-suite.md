# CC-Suite Configuration

Project-specific settings for cc-suite commands.
Generated by `/init`. Edit freely — all fields are optional.

## Project

- **Stack**: Hugo static site (theme `yunze`) + Tiptap (npm, via Hugo js.Build) + Cloudflare Workers (JS)
- **Test command**: hugo --minify (no unit-test suite; build is the smoke test)
- **Source directories**: themes/, layouts/, assets/, static/, content/, data/

## Defaults

These override the built-in defaults for all commands in this project.
Remove a line to fall back to the built-in default.

- **Default model**: gpt-5.5
- **Default effort**: high
- **Default audit type**: mini
- **Default sandbox**: workspace-write

## Audit Focus

balanced

Additional instructions appended to every audit's developer-instructions:

```text
Give equal attention to all audit dimensions.
```

## Skip Patterns

Files and directories to always skip during audits (glob patterns):

```text
node_modules/
public/
resources/
dist/
build/
*.min.js
*.bundle.js
*.lock
package-lock.json
.git/
themes/yunze/assets/js/vendor/
```

## Project-Specific Instructions

Custom instructions appended to Codex's developer-instructions for every command.
Use this to tell Codex about your project's conventions, architecture, or constraints.

```text
This is a bilingual (zh/en) Hugo blog deployed to GitHub Pages at duyunze.com, with Cloudflare Workers backing the time-capsule, comments, voice-diary, and CMS-auth features. Content lives in a separate private repo (Yunze229/yunze-private); this public repo holds only code, theme, workers, and deploy workflows. Any user-visible string MUST be correct in BOTH Chinese and English (see the bilingual class pairs: lang-zh/lang-en, post-zh/post-en). Worker source dirs (static/capsule-worker/, static/cms-auth-worker/) handle auth/session via HttpOnly cookies + Cloudflare KV — treat secrets and session logic as security-sensitive. Follow existing patterns in themes/yunze/ and the Worker code; do not introduce a build step or framework the project doesn't already use.
```
